"use strict";

function systoleDiastole(payload) {
  payload.descriptor.value = function (durationFraction) {
    this.duration = this.duration * durationFraction;
  };
}

module.exports = systoleDiastole;